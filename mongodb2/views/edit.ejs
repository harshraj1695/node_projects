<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
   <div class="min-h-screen flex items-center justify-center p-4">
       <div class="w-full max-w-md bg-zinc-800 p-6 rounded-lg shadow-md">
            <div class="nav mb-8">
                <a href="/read" class="text-blue-500 hover:underline">Read Users</a>
            </div>
            <h1 class="text-4xl font-semibold mb-6 text-center">Update User</h1>
            <form action="/update/<%= user._id %>" method="post" class="space-y-4">
                <input class="w-full px-5 py-3 border border-zinc-700 rounded-lg bg-transparent text-white placeholder-gray-400 focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-50" type="text" name="name" placeholder="Name" value="<%= user.name %>">
                <input class="w-full px-5 py-3 border border-zinc-700 rounded-lg bg-transparent text-white placeholder-gray-400 focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-50" type="email" name="email" placeholder="Email" value="<%= user.email %>" >
                <input class="w-full px-5 py-3 border border-zinc-700 rounded-lg bg-transparent text-white placeholder-gray-400 focus:border-blue-500 focus:ring focus:ring-blue-500 focus:ring-opacity-50" type="text" name="image" placeholder="Image URL" value="<%= user.image %>">
                <input class="w-full py-3 bg-yellow-500 text-black rounded-lg text-white font-semibold hover:bg-blue-600 transition-colors cursor-pointer" type="submit" value="Update User">
            </form>
       </div>
   </div>
</body>
</html>
