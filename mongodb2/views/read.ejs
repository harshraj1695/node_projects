<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white">
    <div class="min-h-screen p-6">
        <a class="text-blue-500 hover:underline" href="/">Back To Home</a>
        <h1 class="text-3xl mt-6 mb-4 tracking-tighter">All Users</h1>
        <div class="users grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            <% if(users.length > 0) { %>
                <% users.forEach(function (user) { %>
                    <div class="user p-4 bg-zinc-800 rounded-lg">
                        <div class="w-full h-48 bg-zinc-700 rounded-lg overflow-hidden">
                            <img class="w-full h-full object-cover" src="<%= user.image %>" alt="<%= user.name %>">
                        </div>
                        <h3 class="text-2xl mt-3 tracking-tight truncate"><%= user.name %></h3>
                        <h5 class="text-zinc-500 truncate"><%= user.email %></h5>
                        <div class="flex gap-3 mt-6">
                            <a href="/edit/<%= user._id %>" class="text-zinc-300 hover:underline">Edit</a>
                            <a href="/delete/<%= user._id %>" class="text-red-500 hover:underline">Delete</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-zinc-500">No users created</h3>
            <% } %>
        </div>
    </div>
</body>

</html>
